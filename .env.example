# Groq model access for classification. Empty = retrieval-only baseline, no network model calls.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Omit QDRANT_URL to use the in-memory client (fine up to ~50k rulings).
QDRANT_URL=
QDRANT_API_KEY=
